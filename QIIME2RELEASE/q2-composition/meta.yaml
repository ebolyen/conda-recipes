package:
  name: q2-composition
  version: "0.0.5"

source:
  fn: 0.0.5.zip
  url: https://github.com/mortonjt/q2-composition/archive/0.0.5.zip
  # md5: 
#  patches:
   # List any patch files here
   # - fix.patch

build:
  # noarch_python: True
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - q2-composition = q2-composition:main
    #
    # Would create an entry point called q2-composition that calls q2-composition.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - qiime >=2.0.5
    - q2-types >=0.0.5
    - bokeh
    - biom-format >=2.1.5,<2.2.0
    - scipy
    - scikit-bio

  run:
    - python
    - setuptools
    - qiime >=2.0.5
    - q2-types >=0.0.5
    - bokeh
    - biom-format >=2.1.5,<2.2.0
    - scipy
    - scikit-bio

test:
  # Python imports
  imports:
    - q2_composition
    - q2_composition.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.qiime.org
  license: BSD-3-Clause
  summary: 'Compositional statistics plugin for QIIME2.'
  license_family: BSD

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
